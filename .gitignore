node_modules/
.next/
.env
.env.local
*.env
dist/
build/
.DS_Store
.vercel

# Expo local state (devices.json, prebuild cache, web image cache)
.expo/

# Firebase Admin SDK service account — must be loaded from env, never committed
apps/api/src/utils/firebase-service-account.json
